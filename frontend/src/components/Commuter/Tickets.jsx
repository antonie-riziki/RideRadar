import React from 'react'

export default function Tickets(){
  return (
    <div>
      <section className="card">
        <h3>Active Ticket</h3>
        <p>QR / code placeholder. Shows expiry and offline-friendly view.</p>
      </section>

      <section className="card">
        <h3>Ticket history</h3>
        <p>List of recent tickets (simple list, minimal UI).</p>
      </section>
    </div>
  )
}
